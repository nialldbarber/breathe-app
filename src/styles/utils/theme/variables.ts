import { heightPercentageToDP as hp } from 'react-native-responsive-screen';
import { AnyStyles } from '@/styles/types';

const theme: AnyStyles = {
  bottomBarHeight: hp('8%'),
};

export { theme };
